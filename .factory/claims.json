[
  {
    "id": "demo-sandbox",
    "claim": "Try it with sample data; nothing is saved.",
    "where": "landing primary action and demo banner",
    "test": "npm run test:e2e -- --grep @claim:demo-sandbox",
    "sandbox": "fresh browser context at /demo/; save a sample pronunciation, assert only the demo namespace is used, then reset it"
  },
  {
    "id": "no-code-upload",
    "claim": "Code stays in your browser during the demo.",
    "where": "landing facts and privacy section",
    "test": "npm run test:e2e -- --grep @claim:no-code-upload",
    "sandbox": "record every request while editing and listening to sample code; assert every request is same-origin"
  },
  {
    "id": "local-voice",
    "claim": "The reader prefers a voice marked local by your browser.",
    "where": "reader preview and privacy section",
    "test": "npm test -- --grep @claim:local-voice",
    "sandbox": "voice fixture with a network English voice and a local English voice; assert the local voice is selected"
  },
  {
    "id": "offline-reload",
    "claim": "Works offline after the first visit.",
    "where": "landing facts",
    "test": "npm run test:e2e -- --grep @claim:offline-reload",
    "sandbox": "load /demo/, wait for service-worker control, then disable network and reload the demo"
  },
  {
    "id": "free-download",
    "claim": "Free to download. No account.",
    "where": "landing facts and install section",
    "test": "npm run test:e2e -- --grep @claim:free-download",
    "sandbox": "fresh unauthenticated browser context downloads the packaged browser extension"
  }
]
